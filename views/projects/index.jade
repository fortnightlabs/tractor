include ../header
  h1 projects

ul.projects
  - each project in projects
    li
      a( href='/projects/' + project.id )= project.name
      - if (project.rate)
        = ' ($' + project.rate + '/hr)'

a( href='/projects/new' ) new
